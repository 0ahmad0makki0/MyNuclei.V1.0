info:
  name: Idor Iterate URLs
  author: Ahmed Makki
  severity: medium
  description: Iterate through URLs with numbers from 1 to 1000.
  metadata:
    verified: true
    fofa-query: title="BSPHP"
  tags: urls, idor

templates:
  - name: dynamic-path-scan
    path:
      - "{{BaseURL}}/{{i}}"
    method: GET
    retries: 1
    checks:
      - type: code
        compare:
          - 200
    # The loop to generate paths from 1 to 1000
    iterate:
      variable: i
      list: "1-1000"
